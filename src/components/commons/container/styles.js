import {StyleSheet} from 'react-native';
import {hp, SCREEN_WIDTH} from '../../../assets/styles/variables';

export default StyleSheet.create({
  container: {
    flex: 1,
    // width: SCREEN_WIDTH,
    // paddingHorizontal: hp(2),
    // paddingVertical: hp(2), // ****Pendiente que afecta otras vistas****
    backgroundColor: 'transparent',
  },
});
